# CPU:  pip install -r requirements.txt
# NVIDIA GPU (much faster): install the CUDA build of torch first, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
numpy
scipy
soundfile
torch
